<% if signed_in?%>
  <% if current_user.following?(user) %>
    <%= form_with url: user_followee_path(id: user.id, user_nickname: user.nickname), method: :delete do |f| %>
      <%= f.submit 'フォロー解除',class: 'btn btn-primary' %>
    <% end %>
  <% else %>
    <%= form_with url: user_followees_path(user.nickname) do |f| %>
      <%= f.submit 'フォローする',class: 'btn btn-primary' %>
    <% end %>
  <% end %>
<% end %>